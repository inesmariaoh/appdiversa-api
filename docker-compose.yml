services:
  backend:
    build: .
    container_name: appdiversa_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env

  redis:
    image: redis:7-alpine
    container_name: appdiversa_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  celery_worker:
    build: .
    container_name: appdiversa_celery
    command: celery -A appdiversa_core worker --loglevel=info
    volumes:
      - .:/app
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env

  db:
    image: mysql:8.0
    container_name: appdiversa_mysql
    ports:
      - "3307:3306"
    environment:
      MYSQL_DATABASE: appdiversa
      MYSQL_USER: appdiversa_user
      MYSQL_PASSWORD: appdiversa_pass
      MYSQL_ROOT_PASSWORD: root_pass
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/mysql/init-grants.sql:/docker-entrypoint-initdb.d/init-grants.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s

volumes:
  mysql_data:
  redis_data:
